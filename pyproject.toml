[tool.black]
line-length = 100
target-version = ['py38', 'py39']

[tool.isort]
profile = "black"
py_version = 38
line_length = 100
combine_as_imports = true
src_paths = ["disnake"]
skip = ["examples", "tests"]